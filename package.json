{
  "name": "esuyo-web",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "analyze": "echo 'Run: npm run build && npx bundle-size app.js styles.css config.js'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
